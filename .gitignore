node_modules/
dist/
.tmp/
.worktrees/
__pycache__/
*.pyc
.venv/
coverage/
*.log
.DS_Store

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Partial model downloads (ModelStore download algorithm, spec §51)
*.part

# Model binaries and runtime artifacts are never committed to git; they are
# downloaded at runtime and checksummed against defaults/ manifests (§50/§53).
*.bin
# recorded level-frame fixtures for the audio.sock parser tests
!tests/fixtures/levels/*.bin
bin/*
!bin/README.md

# Internal project documentation stays local, out of the public repo
docs/

# AI/agent tooling artifacts stay local
.opencode/
.kilo/
.kilo*
.claude/
CLAUDE.md
AGENTS.md
GEMINI.md
.cursor/
.cursorrules
.windsurf/
.aider*
.serena/
.zcode/
.mcp.json
.specstory/
